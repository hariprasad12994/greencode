#include <vector>
#include <iostream>

int find_execution_time(const std::vector<char>& tasks, std::size_t curr_idx) {
  if(curr_idx => tasks.size()) return 0;


}

int find_execution_time(const std::vector<char>& tasks) {
  return find_execution_time(tasks, 0);
}

int main(void) {
  return 0;
}
